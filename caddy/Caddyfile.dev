{
    local_certs
    debug

    log default {
        output stdout
        format console
        level DEBUG
    }
}

{$DOMAIN_NAME} {
    reverse_proxy fastapi:8000
    tls internal {
        on_demand
    }
}